from gui import ScootCheckApp

def main():
    # On instancie l'application (la fenêtre)
    app = ScootCheckApp()
    # On lance la boucle principale (qui maintient la fenêtre ouverte)
    app.mainloop()

if __name__ == "__main__":
    main()